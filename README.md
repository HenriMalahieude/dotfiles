# Heph's Dotfiles

Expects Neovim version 0.12 or above.
If you go with 0.9 (what Ubuntu 24.1 installs by default) everything except `noice.nvim` and `satellite.nvim` will work.
That's alright, but there are some niceties that come from those plugins.
